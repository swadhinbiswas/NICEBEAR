[package]
name = "nicebear"
version = "0.1.0"
edition = "2021"
description = "NiceBear Rust SDK — Dynamic Avatar Infrastructure"
license = "Apache-2.0"
homepage = "https://nicebear.dev"

[dependencies]
reqwest = { version = "0.12", features = ["blocking", "json", "rustls-tls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
